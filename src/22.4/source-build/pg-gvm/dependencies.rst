.. code-block::
  :caption: Debian required dependencies for pg-gvm

  sudo apt install -y \
    libglib2.0-dev \
    postgresql-server-dev-13 \
    libical-dev

.. code-block::
  :caption: Fedora required dependencies for pg-gvm

  sudo dnf install -y \
    glib2-devel \
    gnutls-devel \
    postgresql-server-devel \
    libical-devel

