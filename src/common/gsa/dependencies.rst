.. code-block::
  :caption: Required dependencies for gsad

  sudo apt install -y \
    libmicrohttpd-dev \
    libxml2-dev \
    libglib2.0-dev \
    libgnutls28-dev

.. code-block::
  :caption: Required dependencies for GSA

  sudo apt install -y \
    nodejs \
    yarnpkg
