.. code-block::
  :caption: Required dependencies for openvas-scanner

  sudo apt install -y \
    bison \
    libglib2.0-dev \
    libgnutls28-dev \
    libgcrypt20-dev \
    libpcap-dev \
    libgpgme-dev \
    libksba-dev \
    rsync \
    nmap

.. code-block::
  :caption: Optional dependencies for openvas-scanner

  sudo apt install -y \
    python-impacket \
    libsnmp-dev
