.. code-block::
  :caption: Required dependencies for gvm-libs

  sudo apt install -y \
    libglib2.0-dev \
    libgpgme-dev \
    libgnutls28-dev \
    uuid-dev \
    libssh-gcrypt-dev \
    libhiredis-dev \
    libxml2-dev \
    libpcap-dev \
    libnet1-dev

.. code-block::
  :caption: Optional dependencies for gvm-libs

  sudo apt install -y \
    libldap2-dev \
    libradcli-dev

